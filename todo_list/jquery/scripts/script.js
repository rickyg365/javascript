// Wait for document to be ready
$(document).ready(function(){

    // jQuery methods go here...
    let addTask = (parent_element_selector) => {
        let new_task = $(parent_element_selector).append();
    };

  
});